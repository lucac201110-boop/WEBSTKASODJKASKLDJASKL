import SectionReveal from "@/components/SectionReveal"; import AnimatedCounter from "@/components/AnimatedCounter"; import { getPublicStats } from "@/lib/api";
export default async function StatsSection(){const stats=await getPublicStats();const items=[{value:stats.member_count,suffix:"+",label:"Community members"},{value:stats.active_license_count,suffix:"",label:"Active licenses"}];return <section className="border-y border-line bg-bg-secondary/60 py-16"><div className="mx-auto max-w-6xl px-6"><div className="grid grid-cols-2 gap-8">{items.map((s,i)=><SectionReveal key={s.label} delay={i*.08} className="text-center"><div className="text-3xl font-bold tracking-tight sm:text-4xl"><AnimatedCounter value={s.value} suffix={s.suffix}/></div><div className="mt-2 text-[13px] text-ink-secondary">{s.label}</div></SectionReveal>)}</div></div></section>}
