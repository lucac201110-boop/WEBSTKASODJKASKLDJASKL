"use client";
import { useState } from "react"; import { motion,AnimatePresence } from "framer-motion"; import { Plus } from "lucide-react"; import { faqs } from "@/lib/data"; import SectionReveal from "@/components/SectionReveal";
export default function FaqSection(){const [open,setOpen]=useState(0);return <section id="faq" className="py-28"><div className="mx-auto max-w-2xl px-6"><SectionReveal className="mx-auto mb-14 max-w-xl text-center"><span className="font-mono text-[11px] uppercase tracking-[0.2em] text-accent">FAQ</span><h2 className="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">Common questions</h2></SectionReveal><div className="space-y-3">{faqs.map((f,i)=>{const isOpen=open===i;return <SectionReveal key={f.question} delay={i*.05}><div className="overflow-hidden rounded-xl border border-line bg-card"><button onClick={()=>setOpen(isOpen?-1:i)} className="flex w-full items-center justify-between px-5 py-4 text-left"><span className="text-[14px] font-medium">{f.question}</span><motion.span animate={{rotate:isOpen?45:0}}><Plus size={17}/></motion.span></button><AnimatePresence initial={false}>{isOpen&&<motion.div initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden"><p className="px-5 pb-4 text-[13.5px] leading-relaxed text-ink-secondary">{f.answer}</p></motion.div>}</AnimatePresence></div></SectionReveal>})}</div></div></section>}
