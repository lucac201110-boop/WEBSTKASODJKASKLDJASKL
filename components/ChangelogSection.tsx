"use client";
import { useEffect,useState } from "react"; import { motion,AnimatePresence } from "framer-motion"; import { ChevronDown } from "lucide-react"; import { getChangelog,type ChangelogEntry } from "@/lib/api"; import SectionReveal from "@/components/SectionReveal";
export default function ChangelogSection(){const [entries,setEntries]=useState<ChangelogEntry[]|null>(null);const [open,setOpen]=useState(0);useEffect(()=>{getChangelog().then(setEntries)},[]);return <section id="changelog" className="py-28"><div className="mx-auto max-w-3xl px-6"><SectionReveal className="mx-auto mb-14 max-w-xl text-center"><span className="font-mono text-[11px] uppercase tracking-[0.2em] text-accent">Changelog</span><h2 className="mt-3 text-3xl font-bold tracking-tight sm:text-4xl">What&apos;s new</h2></SectionReveal>{entries===null?<div className="space-y-3">{[0,1,2].map(i=><div key={i} className="h-14 animate-pulse rounded-xl border border-line bg-card"/>)}</div>:entries.length===0?<p className="text-center text-[14px] text-ink-secondary">No changelog entries yet — check back soon.</p>:<div className="relative border-l border-line pl-8">{entries.map((entry,i)=>{const isOpen=open===i;return <SectionReveal key={entry.version} delay={i*.06} className="relative mb-6 last:mb-0"><span className="absolute -left-[41px] top-1.5 h-2.5 w-2.5 rounded-full border-2 border-bg bg-accent"/><button onClick={()=>setOpen(isOpen?-1:i)} className="flex w-full items-center justify-between rounded-xl border border-line bg-card px-5 py-4 text-left"><div className="flex items-center gap-3"><span className="font-mono text-[14px] font-semibold">v{entry.version}</span><span className="text-[12px] text-ink-secondary">{entry.date}</span></div><ChevronDown size={16} className={`text-ink-secondary ${isOpen?"rotate-180":""}`}/></button><AnimatePresence initial={false}>{isOpen&&<motion.div initial={{height:0,opacity:0}} animate={{height:"auto",opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden"><ul className="ml-5 mt-2 space-y-1.5 py-3">{entry.entries.map(line=><li key={line} className="list-disc text-[13.5px] text-ink-secondary">{line}</li>)}</ul></motion.div>}</AnimatePresence></SectionReveal>})}</div>}</div></section>}
